source "https://rubygems.org"

# Use the github-pages gem so local builds match GitHub's build environment.
gem "github-pages", group: :jekyll_plugins

# Needed for Ruby 3.x where these were removed from the standard library.
gem "webrick", "~> 1.8"

gem "jekyll-feed", "~> 0.17", group: :jekyll_plugins
